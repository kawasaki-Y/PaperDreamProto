# Paper Dream - パーティカードゲーム創作画面の修正

現在のパーティカードゲーム創作画面に以下の問題があります。修正してください。

---

## 問題点

1. **DBとの連携が実装されていない**
   - カード保存ボタンを押してもDBに保存されない
   - ゲーム情報とカード情報の紐付けができていない

2. **ワークフローが不適切**
   - いきなりカード詳細入力になっている
   - ゲーム全体の情報を入力する画面がない
   - カード一覧が表示されない

3. **カードデザイン機能が不足**
   - 表面・裏面のレイアウト調整ができない
   - 画像挿入機能がない
   - リアルタイムプレビューがない

---

## 修正後の正しいワークフロー

### STEP1: ゲーム作成画面

ユーザーが「パーティカードゲーム」を選択した直後に表示:
```
┌─────────────────────────────────────────┐
│ 新しいパーティカードゲームを作成        │
├─────────────────────────────────────────┤
│                                         │
│ ゲーム名                                │
│ ┌─────────────────────────────────┐   │
│ │ 例: UNOっぽいゲーム               │   │
│ └─────────────────────────────────┘   │
│                                         │
│ ゲーム説明                              │
│ ┌─────────────────────────────────┐   │
│ │ 例: 手札を早く無くした人が勝ち   │   │
│ └─────────────────────────────────┘   │
│                                         │
│        [キャンセル]  [ゲームを作成]      │
└─────────────────────────────────────────┘
```

**処理:**
```typescript
const createGame = async () => {
  const response = await fetch('/api/games', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      title: gameTitle,
      description: gameDescription,
    }),
  })
  const game = await response.json()
  // game.id を state に保存
  setCurrentGameId(game.id)
  // STEP2へ遷移
}
```

---

### STEP2: カード管理画面

ゲーム作成後、カード一覧と追加ボタンを表示:
```
┌─────────────────────────────────────────────────────────┐
│ PAPER DREAM                   [← 戻る] [白][黒][HB]      │
├─────────────────────────────────────────────────────────┤
│ ゲーム: UNOっぽいゲーム                                  │
│ 説明: 手札を早く無くした人が勝ち                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ 作成済みカード一覧 (3枚)               [+ 新規カード]    │
│                                                         │
│ ┌──────────┐ ┌──────────┐ ┌──────────┐              │
│ │リバース   │ │スキップ   │ │ドロー2    │              │
│ │カード     │ │カード     │ │カード     │              │
│ │          │ │          │ │          │              │
│ │ [編集]    │ │ [編集]    │ │ [編集]    │              │
│ │ [削除]    │ │ [削除]    │ │ [削除]    │              │
│ └──────────┘ └──────────┘ └──────────┘              │
│                                                         │
│                        [印刷プレビューへ]                │
└─────────────────────────────────────────────────────────┘
```

---

### STEP3: カード作成・編集画面(2ページ構成)

「+ 新規カード」または「編集」をクリックした時に表示:

#### 3-1. カード詳細入力(左側パネル)
```
┌─────────────────────────────────────────┐
│ パーティカード詳細                       │
├─────────────────────────────────────────┤
│                                         │
│ T カード名                              │
│ ┌─────────────────────────────────┐   │
│ │ リバースカード                    │   │
│ └─────────────────────────────────┘   │
│                                         │
│ ✂ カードタイプ                          │
│ [アクション] [イベント] [ペナルティ]     │
│                                         │
│ ◉ アクション内容                        │
│ ┌─────────────────────────────────┐   │
│ │ 手札を全て公開し、他のプレイヤー │   │
│ │ と1枚交換する。                  │   │
│ └─────────────────────────────────┘   │
│                                         │
│ ⚡ 効果説明                             │
│ ┌─────────────────────────────────┐   │
│ │ 交換後、もう1ターン行動できる。  │   │
│ └─────────────────────────────────┘   │
│                                         │
│ ◎ 勝利条件                              │
│ ┌─────────────────────────────────┐   │
│ │ 手札が全て無くなったプレイヤー   │   │
│ │ が勝利                           │   │
│ └─────────────────────────────────┘   │
│                                         │
│ 👥 プレイ人数                           │
│ ┌─────────────────────────────────┐   │
│ │ 3〜6人                           │   │
│ └─────────────────────────────────┘   │
│                                         │
│ 📊 難易度                               │
│ [かんたん] [ふつう] [むずかしい]         │
│                                         │
│      [キャンセル]  [保存して戻る]        │
└─────────────────────────────────────────┘
```

#### 3-2. カードデザイン(右側パネル)
```
┌─────────────────────────────────────────┐
│ カードプレビュー                         │
├─────────────────────────────────────────┤
│                                         │
│ 表面 / 裏面 切り替え: [表面] [裏面]      │
│                                         │
│ ┌───────────────────────────────┐     │
│ │  リバースカード                 │     │
│ │                                 │     │
│ │  [画像エリア]                   │     │
│ │  ┌─────────────────────┐     │     │
│ │  │                         │     │     │
│ │  │   [画像をアップロード]   │     │     │
│ │  │                         │     │     │
│ │  └─────────────────────┘     │     │
│ │                                 │     │
│ │  アクション                     │     │
│ │  手札を全て公開し、他のプレイヤー│     │
│ │  と1枚交換する。                │     │
│ │                                 │     │
│ │  効果                           │     │
│ │  交換後、もう1ターン行動できる。│     │
│ │                                 │     │
│ │  ────────────────────────    │     │
│ │  3〜6人  ふつう                 │     │
│ │                                 │     │
│ │         PARTY CARD              │     │
│ └───────────────────────────────┘     │
│                                         │
│ レイアウト調整                           │
│ - テキストサイズ: [小][中][大]           │
│ - 背景色: [選択]                         │
│ - フォント: [選択]                       │
│                                         │
└─────────────────────────────────────────┘
```

---

## DB保存処理の実装

### カード保存時の処理
```typescript
const saveCard = async () => {
  // attributesにパーティカードゲーム固有の情報を格納
  const attributes = {
    type: selectedCardType, // "action" | "event" | "penalty"
    category: cardCategory,
    action: actionContent,
    effect: effectDescription,
    winCondition: winCondition,
    players: playerCount,
    difficulty: selectedDifficulty,
  }
  
  const response = await fetch('/api/cards', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      gameId: currentGameId, // STEP1で作成したGame ID
      name: cardName,
      description: effectDescription,
      imageUrl: uploadedImageUrl || '',
      attributes: attributes, // JSON形式で保存
    }),
  })
  
  const savedCard = await response.json()
  
  // カード管理画面(STEP2)に戻る
  router.push(`/deckbuilding/manage?gameId=${currentGameId}`)
}
```

### カード読み込み時の処理
```typescript
const loadCards = async (gameId: string) => {
  const response = await fetch(`/api/cards?gameId=${gameId}`)
  const cards = await response.json()
  
  // attributesからパーティカードゲーム固有情報を取得
  const parsedCards = cards.map((card: any) => ({
    id: card.id,
    name: card.name,
    type: card.attributes?.type,
    category: card.attributes?.category,
    action: card.attributes?.action,
    effect: card.attributes?.effect,
    winCondition: card.attributes?.winCondition,
    players: card.attributes?.players,
    difficulty: card.attributes?.difficulty,
    imageUrl: card.imageUrl,
  }))
  
  setCards(parsedCards)
}
```

---

## 画像アップロード機能

### 画像アップロードAPI

`app/api/upload/route.ts`
```typescript
import { NextResponse } from 'next/server'
import { writeFile } from 'fs/promises'
import { join } from 'path'

export async function POST(request: Request) {
  try {
    const formData = await request.formData()
    const file = formData.get('file') as File
    
    if (!file) {
      return NextResponse.json({ error: 'ファイルがありません' }, { status: 400 })
    }
    
    const bytes = await file.arrayBuffer()
    const buffer = Buffer.from(bytes)
    
    // public/uploads に保存
    const filename = `${Date.now()}-${file.name}`
    const path = join(process.cwd(), 'public', 'uploads', filename)
    await writeFile(path, buffer)
    
    // URLを返す
    const imageUrl = `/uploads/${filename}`
    return NextResponse.json({ imageUrl })
  } catch (error) {
    return NextResponse.json({ error: '画像のアップロードに失敗しました' }, { status: 500 })
  }
}
```

### 画像アップロードUI
```typescript
const handleImageUpload = async (e: React.ChangeEvent<HTMLInputElement>) => {
  const file = e.target.files?.[0]
  if (!file) return
  
  const formData = new FormData()
  formData.append('file', file)
  
  const response = await fetch('/api/upload', {
    method: 'POST',
    body: formData,
  })
  
  const { imageUrl } = await response.json()
  setCardImageUrl(imageUrl)
}
```

---

## 実装優先順位

1. **STEP1: ゲーム作成画面** (30分)
   - ゲーム名・説明入力
   - DB保存処理

2. **STEP2: カード管理画面** (30分)
   - カード一覧表示
   - 新規カード追加ボタン
   - 編集・削除ボタン

3. **STEP3: カード作成・編集画面** (1時間)
   - 左側: 詳細入力
   - 右側: プレビュー
   - DB保存処理(attributes活用)

4. **画像アップロード機能** (30分)
   - アップロードAPI
   - プレビュー表示

**合計: 2.5時間**

---

## 完成イメージ

1. ユーザーが「パーティカードゲーム」選択
2. STEP1: ゲーム作成画面 → DB保存 → gameIdを取得
3. STEP2: カード管理画面に遷移
4. 「+ 新規カード」クリック
5. STEP3: カード作成画面
   - 左側で詳細入力
   - 右側でリアルタイムプレビュー
   - 画像アップロード
6. 「保存して戻る」→ DB保存(attributes活用)→ STEP2に戻る
7. カード一覧に追加されたカードが表示
8. 必要なカードを全て作成
9. 「印刷プレビューへ」→ 出力最適化モジュールへ

この修正を実装してください。